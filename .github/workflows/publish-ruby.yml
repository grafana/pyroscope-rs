name: Push ruby gems

on:
  workflow_dispatch:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write

jobs:
  push-ruby-gems:
    name: "Push ruby gems"
    runs-on: ${{ github.repository_owner == 'grafana' && 'ubuntu-x64-small' || 'ubuntu-latest' }}
    if: "startsWith(github.event.release.tag_name, 'ruby-')"
    outputs:
      files_json: ${{ steps.list-files.outputs.files_json }}
    steps:
      - name: Get secrets from Vault
        uses: grafana/shared-workflows/actions/get-vault-secrets@c40cd34c3b8982cf51cad94f0b339ff447ab2cc2 # main
        with:
          repo_secrets: |
            RUBYGEMS_API_KEY=publishing:rubygems_api_key
      - uses: robinraju/release-downloader@daf26c55d821e836577a15f77d86ddc078948b05 # 1.12
        with:
          tag: ${{ github.event.release.tag_name }}
          fileName: "*"
          tarBall: false
          zipBall: false
          out-file-path: "dist"
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: ruby/setup-ruby@19a43a6a2428d455dbd1b85344698725179c9d8c # v1.289.0
        with:
          ruby-version: '4.0.1'
      - run: |
          set -ex
          cd dist
          ls
          for gem in *.gem;
          do
            gem push "${gem}";
            sleep 5;
          done
        env:
          GEM_HOST_API_KEY: ${{ env.RUBYGEMS_API_KEY }}


