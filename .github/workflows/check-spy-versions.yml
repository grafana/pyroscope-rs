name: Check spy versions

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  check-spy-versions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify spy version sync
        run: ./ci/check-spy-versions.sh
