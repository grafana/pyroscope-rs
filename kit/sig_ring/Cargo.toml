[package]
name = "sig_ring"
version = "2.0.0"
edition = "2024"
publish = false

[features]
default = []
ring-512k = []
ring-1m = []

[dependencies]
bbqueue = "0.5"
python_unwind = { path = "../python_unwind" }
